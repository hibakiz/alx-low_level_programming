#include<stdio.h>
/**
 * main - Entry point
 *
 * Description: 'tsks'
 *
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	/**
	* long int x;
	*
	*x = 612852475143;
	*i = x - 1;
	* while (i < x)
	* {
	*	if (x % i == 0)
	*	{
	*	printf("%lu", i);
	*	break;
	*	}
	*	i--;
	* }
	*/
	printf("%d\n", 3 * 4019);
	return (0);
}
