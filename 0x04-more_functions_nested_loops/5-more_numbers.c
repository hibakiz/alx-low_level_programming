#include "main.h"
#include<stdio.h>
/**
 * more_numbers- Entry point
 *
 * Description: 'tsks'
 *
 *
 * Return: Always 0 (Success)
 */
void more_numbers(void)
{
	int i, x;

	for (x = 0; x < 10; x++)
	{
		for (i = 0; i <= 14; i++)
		{
			printf("%d", i);
		}
		printf("\n");
	}
}
